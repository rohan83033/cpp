#include <stdio.h>
int main()
{
    printf("size is int %d \n", sizeof(int));
    printf("size is char %c \n", sizeof(char));
    printf("size is float %f \n", sizeof(float));
    printf("size is long %ld \n", sizeof(long));
    
    return 0;
}